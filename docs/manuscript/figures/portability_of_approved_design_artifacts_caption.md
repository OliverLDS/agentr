Figure 7. Portability of approved design artifacts.

The diagram shows that `agentr` outputs portable design artifacts, especially the approved `AgentSpec` and approved workflow, which can be handed off to implementation prompts, coding agents, and downstream implementations in different stacks. The portability claim applies to the design artifact rather than to a runtime inherited from `agentr`.
