% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/task_specs.R
\name{render_task_previews}
\alias{render_task_previews}
\title{Render task-local design-review previews under a workspace}
\usage{
render_task_previews(
  root,
  tasks_dir = "tasks",
  docs_dir = "docs",
  recursive = TRUE,
  require_workflow = TRUE,
  ...
)
}
\arguments{
\item{root}{Workspace root directory.}

\item{tasks_dir}{Tasks directory relative to \code{root}, or an absolute path.}

\item{docs_dir}{Documentation/spec directory name relative to each task.}

\item{recursive}{Whether to scan nested task folders. Defaults to \code{TRUE} so
node-folder subworkflow specs are rendered too.}

\item{require_workflow}{Whether discovered task previews require a workflow
spec. Discovered paths always have one; this is forwarded to
\code{\link[=render_task_preview]{render_task_preview()}}.}

\item{...}{Additional arguments passed to \code{\link[=render_task_preview]{render_task_preview()}}.}
}
\value{
Data frame with rendered task directories and review paths.
}
\description{
Scans a workspace for \code{workflow_spec.yaml} files under task-local \verb{docs/}
directories and renders one review HTML file per discovered task. This helper
only loads specs and writes review artifacts; it does not execute task code.
}
